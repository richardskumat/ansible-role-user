---
image: qwe1/dind-ansible-molecule:latest

variables:
  DOCKER_DRIVER: overlay2
  DOCKER_TLS_CERTDIR: "/certs"
  ROLE_NAME: ansible-role-user
  GALAXY_USERNAME: richardskumat
  GALAXY_ROLENAME: ansible_role_user

services:
  - docker:dind

stages:
  - test

before_script:
  - ansible --version
  - molecule --version
  - cd ..
  - cp -aR $ROLE_NAME $GALAXY_USERNAME.$GALAXY_ROLENAME
  - cd $GALAXY_USERNAME.$GALAXY_ROLENAME

test_d11:
  stage: test
  variables:
    MOLECULE_DISTRO: "qwe1/docker-debian:11-molecule"
  script:
    - molecule test

test_d12:
  stage: test
  variables:
    MOLECULE_DISTRO: "qwe1/docker-debian:12-molecule"
  script:
    - molecule test

test_f38:
  stage: test
  variables:
    MOLECULE_DISTRO: "fedora:38"
  script:
    - molecule test

test_el9:
  stage: test
  variables:
    # MOLECULE_DISTRO: "registry.access.redhat.com/ubi9"
    MOLECULE_DISTRO: "almalinux:9"
  script:
    - molecule test
